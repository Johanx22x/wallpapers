---
---

<header class="header">
  <h1 class="header-title">WALLPAPERS</h1>
  <p class="header-subtitle">a curated collection</p>
</header>

<style>
  .header {
    text-align: center;
    padding: 80px 24px 48px;
    position: relative;
  }

  .header-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 300;
    letter-spacing: 0.3em;
    color: var(--ctp-text);
    margin-bottom: 8px;
  }

  .header-subtitle {
    font-size: clamp(0.85rem, 1.5vw, 1rem);
    font-weight: 300;
    letter-spacing: 0.15em;
    color: var(--ctp-subtext0);
  }

  @media (max-width: 480px) {
    .header {
      padding: 48px 16px 32px;
    }
  }
</style>
